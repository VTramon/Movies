export type TopMoviesCardProps = {
  items: {
    id?: string;
    title?: string;
    image?: string;
  };
};

export type TopMoviesBoxProps = {
  id?: string;
  title?: string;
  image?: string;
};
